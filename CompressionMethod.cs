namespace ARJArchive
{
    enum CompressionMethod : byte
    {
        Stored = 0,
        CompressedMost = 1,
        Compressed = 2,
        CompressedFaster = 3,
        CompressedFastest = 4
    }
}
